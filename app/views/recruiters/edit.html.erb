<div class="col-6 offset-1 mt-5">
    <div class="shadow card">
        <h5 class="card-header text-center">Edit <%= current_user.first_name %> <%= current_user.last_name %></h5>
        <div class="card-body">
            <p class="card-text">
                <%= render 'partials/recruiter_form' %>
            </p>
        </div>
    </div>
</div>